<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sehyun.backend.db.transaction.AccountMapper">

    <select id="findById"
            parameterType="long"
            resultType="Account">
        SELECT id,
        balance
        FROM account
        WHERE id = #{accountId}
    </select>

    <update id="withdraw">
        UPDATE account
        SET balance = balance - #{amount}
        WHERE id = #{accountId}
    </update>

    <update id="deposit">
        UPDATE account
        SET balance = balance + #{amount}
        WHERE id = #{accountId}
    </update>

</mapper>